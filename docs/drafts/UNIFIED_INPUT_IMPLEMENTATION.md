# 统一输入框实施计划

## 📋 实施步骤（分5步，确保质量）

### 第1步：添加新的变量和计算属性 ✅
- 添加 `quickTaskInput` ref
- 添加 `tempDescription` ref  
- 添加 `displayInputValue` computed
- 添加 `handleInputChange` 方法

### 第2步：修改UI结构 ✅
- 删除第一行的 input（标题输入框）
- 删除第二行的 textarea（描述输入框）
- 添加新的统一输入框
- 保留AI建议气泡
- 保留属性配置区域

### 第3步：修改全屏编辑功能 ✅
- 修改 `openFullscreenDesc` 函数
- 修改全屏编辑器的保存逻辑
- 确保第一行=标题，后续行=描述

### 第4步：更新相关函数 ✅
- 更新 `addTask` 函数
- 更新 `triggerAIAssist` 函数
- 更新 `generateDescription` 函数
- 更新 `scanTextFromCamera` 函数
- 更新 `handleTaskInputBlur` 函数

### 第5步：测试验证 ✅
- 测试快速添加（只有标题）
- 测试详细添加（标题+描述）
- 测试AI功能
- 测试拍照功能
- 测试全屏编辑

## ⚠️ 注意事项

1. **保留所有现有功能**
   - AI建议气泡
   - 属性配置（日期、分类、优先级）
   - 全屏编辑器
   - AI生成描述
   - 拍照识别

2. **确保兼容性**
   - 不影响任务列表显示
   - 不影响任务编辑功能
   - 不影响数据存储

3. **代码质量**
   - 每一步都要测试
   - 保持代码整洁
   - 添加必要注释

## 🎯 预期效果

### UI变化
```
修改前：
┌─────────────────────────────────────┐
│ 任务标题输入框          🤖 📷      │
├─────────────────────────────────────┤
│ 任务描述输入框（可选）              │
│                          🤖 ⛶      │
└─────────────────────────────────────┘

修改后：
┌─────────────────────────────────────┐
│ 输入任务...              🤖 📷 ⛶  │
└─────────────────────────────────────┘
```

### 功能保留
- ✅ AI分类建议（🤖）
- ✅ 拍照识别（📷）
- ✅ 全屏编辑（⛶）
- ✅ 属性配置（日期、分类、优先级）
- ✅ 三个点提示（有描述时）

## 🚀 开始实施

准备就绪，开始第1步...
