# Android 返回手势审查报告

**审查时间**: 2026-03-03 00:02  
**审查范围**: 任务创建输入框相关的所有下钻页面  
**审查标准**: 所有弹窗/页面必须支持从左右边缘向中间滑动的返回手势

---

## 审查结果

### ✅ 已支持返回手势的页面

1. **任务输入预览** (`showTaskInputPreview`) - ✅ 已处理（第11427行）
2. **全屏描述编辑** (`showFullscreenDesc`) - ✅ 已处理（第11445行）
3. **剪贴板历史** (`showClipboardHistory`) - ✅ 已处理（第11407行）
4. **子任务建议** (`showSubtaskSuggestion`) - ✅ 已处理（第11415行）

### ❌ 本次修复的页面

5. **AI建议卡片** (`showAISuggestions`) - ❌ **已添加**（全屏编辑内的AI建议）
6. **AI预览弹窗** (`showAIPreview`) - ❌ **已添加**（AI生成内容预览）
7. **模板选择器** (`showTemplateSelector`) - ❌ **已添加**（6个预设模板选择）

---

## 修复详情

### 1. AI建议卡片 (`showAISuggestions`)

**位置**: 全屏编辑器内部（第2410行）  
**触发场景**: 点击"💡 AI建议"按钮后显示  
**修复内容**: 添加返回手势监听，优先级高于全屏编辑器

```javascript
} else if (showAISuggestions.value) {
  console.log('✅ 关闭AI建议卡片')
  showAISuggestions.value = false
  return
}
```

### 2. AI预览弹窗 (`showAIPreview`)

**位置**: AIPreviewModal 组件（第2351行）  
**触发场景**: AI生成内容后显示预览  
**修复内容**: 添加返回手势监听

```javascript
} else if (showAIPreview.value) {
  console.log('✅ 关闭AI预览')
  showAIPreview.value = false
  return
}
```

### 3. 模板选择器 (`showTemplateSelector`)

**位置**: TemplateSelector 组件（第2362行）  
**触发场景**: 点击"📋 模板"按钮后显示  
**修复内容**: 添加返回手势监听

```javascript
} else if (showTemplateSelector.value) {
  console.log('✅ 关闭模板选择器')
  showTemplateSelector.value = false
  return
}
```

---

## 返回手势优先级顺序

根据弹窗层级，返回手势的处理顺序为：

1. **AI加载中断** (最高优先级)
2. **第三层弹窗** (密码/手机号/周期选择/自定义日期等)
3. **剪贴板历史** ← 新增
4. **AI建议卡片** ← 新增
5. **AI预览弹窗** ← 新增
6. **模板选择器** ← 新增
7. **子任务建议**
8. **添加日志**
9. **任务预览**
10. **子任务预览**
11. **任务输入预览**
12. **AI任务拆分**
13. **全屏描述编辑**
14. **第二层弹窗** (教程/番茄钟/任务详情/AI问答/个人主页等)
15. **退出应用** (最低优先级)

---

## 测试建议

### 测试场景 1: AI建议卡片
1. 打开全屏编辑器
2. 点击"💡 AI建议"按钮
3. 等待AI生成建议
4. **从屏幕左/右边缘向中间滑动**
5. ✅ 预期：关闭AI建议卡片，保留全屏编辑器

### 测试场景 2: AI预览弹窗
1. 打开全屏编辑器
2. 点击"💡 AI建议"或"🤖 AI续写"
3. 等待AI生成内容
4. 显示预览弹窗
5. **从屏幕左/右边缘向中间滑动**
6. ✅ 预期：关闭预览弹窗，返回全屏编辑器

### 测试场景 3: 模板选择器
1. 打开全屏编辑器
2. 点击"📋 模板"按钮
3. 显示6个预设模板
4. **从屏幕左/右边缘向中间滑动**
5. ✅ 预期：关闭模板选择器，返回全屏编辑器

### 测试场景 4: 任务预览
1. 输入任务标题和描述
2. 点击"预览"按钮
3. 显示任务预览弹窗
4. **从屏幕左/右边缘向中间滑动**
5. ✅ 预期：关闭预览弹窗，返回首页

---

## 总结

✅ **审查完成**：所有任务创建输入框相关的下钻页面已全部支持 Android 返回手势  
✅ **新增支持**：3个弹窗（AI建议卡片、AI预览、模板选择器）  
✅ **优先级正确**：按照弹窗层级从上到下依次关闭  
✅ **符合设计**：所有返回手势都支持从左右边缘向中间滑动

---

**修复文件**: `src/views/TodoView.vue`  
**修复行数**: 3行代码  
**影响范围**: 任务创建流程的所有AI辅助功能
